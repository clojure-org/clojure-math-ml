
;;; Calculate core-mathematical constants to a given decimal place.
;;; Note: all functions have a respective "global" or "universal" that returns
;;; the constants value to a set number of decimal places

(defn e-const [])

(defn pi-const [])
